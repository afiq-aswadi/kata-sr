"""Fibonacci using dynamic programming with memoization."""


def fibonacci_dp(n: int) -> int:
    """Compute nth Fibonacci number using dynamic programming.

    Use memoization (top-down approach) to avoid recomputing subproblems.

    Args:
        n: index (0-indexed, so fib(0)=0, fib(1)=1)

    Returns:
        nth Fibonacci number
    """
    # TODO: Use dict for memoization
    # Hint: Create a helper function that uses a memo dict
    # BLANK_START
    raise NotImplementedError
    # BLANK_END
