"""Two sum kata."""


def two_sum(nums: list[int], target: int) -> list[int]:
    """Find indices of two numbers that add up to target.

    Args:
        nums: array of integers
        target: target sum

    Returns:
        list of two indices [i, j] where nums[i] + nums[j] == target
    """
    # TODO: use hash map to find complement in O(n) time
    # BLANK_START
    pass
    # BLANK_END


def two_sum_sorted(nums: list[int], target: int) -> list[int]:
    """Find indices of two numbers in a sorted array that add up to target.

    Use two-pointer technique for O(1) space complexity.

    Args:
        nums: sorted array of integers
        target: target sum

    Returns:
        list of two indices [i, j] where nums[i] + nums[j] == target
    """
    # TODO: use two pointers from both ends
    # BLANK_START
    pass
    # BLANK_END
